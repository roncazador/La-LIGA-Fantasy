(()=>{
'use strict';
const VERSION='3.2.0',API='/api/calendar/autonomous',SEED='/official-fixtures-seed-2026-27.json',REFRESH_MS=15000;
const LIVE=new Set(['1H','2H','HT','LIVE','IN_PLAY','PLAYING','EN DIRECTO','DESCANSO','PAUSED','HALFTIME']);
const FINAL=new Set(['FT','FINISHED','FINAL','ENDED','AET','PEN','COMPLETED']);let renderToken=0,timer=null,observer=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const date=v=>new Date(v);const validDate=v=>Number.isFinite(date(v).getTime());const status=v=>String(v?.short??v?.label??v??'PRÓXIMO').trim().toUpperCase();const isLive=v=>LIVE.has(status(v));const isFinal=v=>FINAL.has(status(v));const score=v=>Number.isInteger(Number(v))&&Number(v)>=0&&Number(v)<=99?Number(v):null;
function fmt(v){const d=date(v);if(!validDate(v))return{day:'N/D',time:'N/D'};return{day:new Intl.DateTimeFormat('es-ES',{weekday:'long',day:'2-digit',month:'long',timeZone:'Europe/Madrid'}).format(d),time:new Intl.DateTimeFormat('es-ES',{hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Europe/Madrid'}).format(d)}}
function normalize(raw,source){if(!raw||typeof raw!=='object')return null;const home=String(raw.home??raw.homeTeam?.name??raw.localTeam?.name??raw.home_team?.name??'').trim(),away=String(raw.away??raw.awayTeam?.name??raw.visitorTeam?.name??raw.away_team?.name??'').trim(),utcDate=raw.utcDate??raw.date??raw.startDate??raw.starting_at??raw.datetime??raw.kickoff;if(!home||!away||!validDate(utcDate))return null;const sources=Array.isArray(raw.sources)&&raw.sources.length?raw.sources:[source||raw.source||'LALIGA oficial'];return{...raw,id:String(raw.id??raw.fixtureId??raw.matchId??`${home}-${away}-${utcDate}`),utcDate:date(utcDate).toISOString(),home,away,status:status(raw.status),matchday:raw.matchday??raw.officialMatchday??raw.gameweek?.week??null,homeScore:score(raw.homeScore??raw.home_score??raw.goals?.home??raw.homeTeam?.score??raw.home_team?.score),awayScore:score(raw.awayScore??raw.away_score??raw.goals?.away??raw.awayTeam?.score??raw.away_team?.score),source:source||raw.source||'LALIGA oficial',sources:[...new Set(sources.map(String))]}}
function key(m){return`${m.utcDate.slice(0,16)}|${m.home.toLowerCase()}|${m.away.toLowerCase()}`}function mergeMatches(input){const map=new Map();for(const raw of Array.isArray(input)?input:[]){const m=normalize(raw,raw?.source);if(!m)continue;const k=key(m),old=map.get(k);if(!old){map.set(k,m);continue}const merged={...old};if(merged.homeScore==null&&m.homeScore!=null)merged.homeScore=m.homeScore;if(merged.awayScore==null&&m.awayScore!=null)merged.awayScore=m.awayScore;if(merged.matchday==null&&m.matchday!=null)merged.matchday=m.matchday;if(isLive(m.status)&&!isFinal(merged.status))merged.status=m.status;if(isFinal(m.status))merged.status=m.status;merged.sources=[...new Set([...(old.sources||[]),...(m.sources||[]),m.source])];map.set(k,merged)}return[...map.values()].sort((a,b)=>date(a.utcDate)-date(b.utcDate))}
async function json(url){const r=await fetch(url,{credentials:'include',cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok)throw Error(`HTTP_${r.status}`);return r.json()}
async function loadData(){try{const d=await json(API),matches=mergeMatches(d?.matches);if(matches.length||d?.fallback)return{version:VERSION,...d,matches,live:matches.filter(m=>isLive(m.status)).length};throw Error('EMPTY_CALENDAR')}catch(error){try{const seed=await json(SEED),matches=mergeMatches((seed?.fixtures||[]).map(m=>({...m,source:'LALIGA oficial · semilla protegida'})));return{version:VERSION,sourcePolicy:'LALIGA oficial · respaldo automático',officialAvailable:false,officialMode:'protected-seed',futbolFantasyAvailable:false,fallback:'protected-seed',matches,live:matches.filter(m=>isLive(m.status)).length,updatedAt:new Date().toISOString(),warning:error.message}}catch(seedError){return{version:VERSION,matches:[],live:0,fallback:'empty',updatedAt:new Date().toISOString(),warning:`${error.message}; ${seedError.message}`}}}
function ensureStyles(){if(document.getElementById('cal32style'))return;const s=document.createElement('style');s.id='cal32style';s.textContent=`#calendar30{background:radial-gradient(circle at 15% 0%,#25385c 0,#121b2b 38%,#090f18 100%);border:1px solid #3a4b69;border-radius:22px;padding:18px;margin:0 0 16px;box-shadow:0 18px 45px rgba(0,0,0,.28)}#calendar30 .c32head{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:16px}.c32title{font-size:24px;font-weight:950}.c32sub{font-size:10px;color:#9da9bd;margin-top:5px}.c32badge{padding:8px 11px;border-radius:999px;background:#182538;border:1px solid #3b4c68;font-size:9px;font-weight:950}.c32live{color:#ff6f78}.c32ok{color:#39db91}.c32hero{display:grid;grid-template-columns:repeat(4,1fr);gap:9px;margin-bottom:15px}.c32pill{padding:11px 12px;border:1px solid #30405a;border-radius:14px;background:rgba(20,31,47,.8)}.c32pill span{display:block;font-size:9px;color:#9da9bd}.c32pill b{display:block;font-size:19px;margin-top:4px}.c32day{display:flex;align-items:center;gap:9px;font-size:10px;color:#9da9bd;text-transform:uppercase;font-weight:950;letter-spacing:.7px;margin:17px 0 8px}.c32day:after{content:'';height:1px;background:#2d3b52;flex:1}.c32match{display:grid;grid-template-columns:minmax(0,1fr) 100px minmax(0,1fr);gap:8px;align-items:center;padding:13px 11px;margin:5px 0;border:1px solid transparent;border-radius:15px;transition:.15s;background:rgba(16,25,39,.42);cursor:pointer}.c32match:hover{border-color:#526a92;background:#172438;transform:translateY(-1px)}.c32team{font-size:13px;font-weight:950}.c32away{text-align:right}.c32mid{text-align:center}.c32score{font-size:21px;font-weight:950}.c32status{font-size:8px;font-weight:950;margin-top:4px}.c32meta{font-size:8px;color:#7fa9e9;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.c32cta{font-size:7px;color:#aab6c9;margin-top:5px}.c32foot{margin-top:13px;padding:11px;border-radius:12px;background:#0c1420;border:1px solid #28364c;font-size:9px;color:#9da9bd;line-height:1.5}@media(max-width:620px){.c32hero{grid-template-columns:repeat(2,1fr)}.c32title{font-size:20px}.c32match{grid-template-columns:minmax(0,1fr) 74px minmax(0,1fr);padding:11px 7px}.c32team{font-size:12px}}`;document.head.appendChild(s)}
function removeLegacy(){document.getElementById('loadFixtures')?.remove();document.getElementById('loadSeed')?.remove();const st=document.getElementById('fixturesStatus');if(st){st.hidden=true;st.setAttribute('aria-hidden','true')}const old=document.getElementById('fixtures');if(old){old.innerHTML='';old.hidden=true;old.setAttribute('aria-hidden','true')}}function host(){return document.getElementById('partidos')}function ensureBox(){const section=host();if(!section)return null;removeLegacy();ensureStyles();let box=document.getElementById('calendar30');if(!box){box=document.createElement('div');box.id='calendar30';box.setAttribute('aria-live','polite');section.prepend(box)}return box}
function openDetail(match){if(window.LALIGA_MATCH_DETAIL?.open){window.LALIGA_MATCH_DETAIL.open(match);return}const d=document.createElement('div');d.textContent='Cargando detalle del partido…';d.style.cssText='position:fixed;inset:0;z-index:99999;background:#0b111c;color:white;display:grid;place-items:center';document.body.appendChild(d)}
function render(data){const box=ensureBox();if(!box)return false;const token=++renderToken,matches=mergeMatches(data?.matches),live=matches.filter(m=>isLive(m.status)),finished=matches.filter(m=>isFinal(m.status)),upcoming=matches.filter(m=>!isFinal(m.status)&&date(m.utcDate)>=new Date()).slice(0,3),grouped=new Map();for(const m of matches){const f=fmt(m.utcDate);if(!grouped.has(f.day))grouped.set(f.day,[]);grouped.get(f.day).push(m)}if(token!==renderToken)return false;box.innerHTML=`<div class="c32head"><div><div class="c32title">⚽ Calendario LaLiga</div><div class="c32sub">Resultados, directo y detalle fantasy · toca cualquier partido para abrir sus datos</div></div><div class="c32badge ${live.length?'c32live':'c32ok'}">${live.length?`● ${live.length} EN DIRECTO`:'● ACTIVO'}</div></div><div class="c32hero"><div class="c32pill"><span>Partidos</span><b>${matches.length}</b></div><div class="c32pill"><span>En directo</span><b class="${live.length?'c32live':''}">${live.length}</b></div><div class="c32pill"><span>Finalizados</span><b>${finished.length}</b></div><div class="c32pill"><span>Próximos</span><b>${upcoming.length}</b></div></div>${[...grouped.entries()].map(([day,items])=>`<div class="c32day">${esc(day)}</div>${items.map(m=>{const f=fmt(m.utcDate),st=status(m.status),has=m.homeScore!=null||m.awayScore!=null,txt=has?`${m.homeScore??0} - ${m.awayScore??0}`:f.time;return`<div class="c32match" data-match-id="${esc(m.id)}"><div class="c32team">${esc(m.home)}</div><div class="c32mid"><div class="c32score">${esc(txt)}</div><div class="c32status ${isLive(st)?'c32live':''}">${isLive(st)?'● EN DIRECTO':isFinal(st)?'FINALIZADO':esc(st)}</div><div class="c32meta">${esc((m.sources||[m.source||'LALIGA oficial']).join(' · '))}</div><div class="c32cta">Tocar para ver alineaciones y posibles puntos</div></div><div class="c32team c32away">${esc(m.away)}</div></div>`}).join('')}`).join('')||'<div class="c32foot">Sin partidos disponibles.</div>'}${data?.warning?`<div class="c32foot">Sincronización: ${esc(data.warning)}. Se mantiene el respaldo automático.</div>`:`<div class="c32foot"><b>Fuente base:</b> calendario oficial. <b>Fantasy:</b> FutbolFantasy se utiliza como contraste público para el detalle, sin convertir estimaciones en puntos oficiales.</div>`}`;box.querySelectorAll('.c32match').forEach((el,i)=>el.addEventListener('click',()=>openDetail(matches.find(m=>String(m.id)===String(el.dataset.matchId))||matches[i])));box.dataset.initialized='1';return true}
async function refresh(){const box=ensureBox();if(!box)return false;if(box.dataset.initialized!=='1')box.innerHTML='<div class="c32foot">⏳ Cargando calendario automáticamente…</div>';try{return render(await loadData())}catch{return render({matches:[],warning:'No se pudo consultar el calendario'})}}
function boot(){ensureStyles();ensureBox();void refresh();if(timer)clearInterval(timer);timer=setInterval(()=>void refresh(),REFRESH_MS);if(observer)observer.disconnect();if(document.body){observer=new MutationObserver(()=>{if(host()&&!document.getElementById('calendar30'))void refresh()});observer.observe(document.body,{childList:true,subtree:true})}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
