import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=f=>fs.readFileSync(f,'utf8');
const index=read('index.html'),connection=read('connection-client.js'),legacy=read('calendar-client.js'),autonomous=read('calendar-autonomous-v30.js'),nextCalendar=read('calendar-autonomous-v34.js'),detail=read('match-detail-ui-v31.js'),pkg=JSON.parse(read('package.json')),state=read('PROJECT_STATE.md'),next=read('NEXT_IMPROVEMENT.md'),sw=read('sw.js'),brain=read('brain-core-v27.mjs'),hook=read('brain-reliability-hook-v29.mjs'),selfHeal=read('scripts/self-heal-agent.mjs'),memory=read('scripts/self-heal-memory-v2.test.mjs');
const checks=[
['brain rejects learning without final:true',brain.includes('sample.final!==true')],['brain confidence independent of weeklyPoints',!brain.includes('f.weeklyPoints!=null?0.10')],['dashboard has one shared state declaration',(index.match(/let state=loadState\(\);/g)||[]).length===1],['legacy calendar disabled',legacy.includes('LALIGA_CALENDAR_LEGACY_DISABLED')],['single calendar loader',connection.includes("loadInline('/calendar-autonomous-v34.js','34')")&&!connection.includes("load('/calendar-autonomous-v30.js','33')")],['legacy calendar remains syntax-compatible',autonomous.includes("const API='/api/calendar/autonomous'")],['new calendar avoids parent clear',!nextCalendar.includes("host.innerHTML=''")],['new calendar observes DOM',nextCalendar.includes('MutationObserver')],['new calendar has one refresh controller',(nextCalendar.match(/setInterval\(/g)||[]).length===1],['new calendar live state',nextCalendar.includes('EN DIRECTO')],['new calendar final state',nextCalendar.includes('FINALIZADO')],['new calendar jornada navigation',nextCalendar.includes('Jornada anterior')&&nextCalendar.includes('Jornada siguiente')],['new calendar date/time Madrid',nextCalendar.includes('Europe/Madrid')],['match rows interactive',nextCalendar.includes('data-match-id')&&nextCalendar.includes('openDetail(m)')],['detail endpoint',detail.includes('/api/futbolfantasy/data')],['detail robust id resolution',detail.includes('String(m.id??\'\')===String(match.id??\'\')')],['detail no fabricated data',detail.includes('no se muestran datos inventados')],['read-only preserved',state.includes('solo lectura')],['self-healing contract',selfHeal.includes("sh('npm',['test']")&&memory.includes('learnCorrection')],['reliability layers',hook.includes('rawConfidence')&&hook.includes('calibratedConfidence')],['PWA caches v34',sw.includes('./calendar-autonomous-v34.js')],['PWA caches detail',sw.includes('./match-detail-ui-v31.js')],['v34 test wired',pkg.scripts.test.includes('calendar-ui-v34.test.mjs')],['roadmap structural-first',/correcci[oó]n estructural/.test(next.toLowerCase())]
];
assert.equal(checks.length,24);for(const[name,ok]of checks)assert.ok(ok,name);console.log('STRUCTURAL AUDIT v31: 24/24 checks passed');
