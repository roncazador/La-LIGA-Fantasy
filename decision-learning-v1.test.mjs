import fs from 'node:fs';import assert from 'node:assert/strict';
const ui=fs.readFileSync('decision-learning-v1.js','utf8');let n=0;const c=(ok,msg)=>{n++;assert.ok(ok,`DL-${String(n).padStart(2,'0')}: ${msg}`)};
c(ui.includes('laliga_decision_learning_v1'),'memoria local versionada');c(ui.includes('La seguí')&&ui.includes('No la seguí'),'seguimiento de recomendación');c(ui.includes('Decisión tomada'),'decisión estructurada');c(ui.includes('Motivo'),'motivo');c(ui.includes('Puntos finales'),'resultado final');c(ui.includes('Cerrar jornada y aprender'),'cierre de jornada');c(ui.includes("status:'pending-final'"),'estado pendiente');c(ui.includes("status:'final'"),'estado final');c(ui.includes('function learn('),'aprendizaje aislado');c(ui.includes('followRate')&&ui.includes('goodRate'),'métricas de decisión');c(ui.includes('trust'),'confianza de preferencias');c(ui.includes('decisionLearningToggle'),'rama accesible desde pestañas');c(ui.includes("r.style.display=show?'block':'none'"),'rama oculta por defecto');c(ui.includes('no escriben directamente en los pesos'),'protección de pesos del Cerebro');c(ui.includes("pts<0||pts>1000"),'validación de puntos');c(ui.includes('Selecciona primero si seguiste'),'no guarda decisión ambigua');console.log(`decision-learning-v1: ${n}/${n} checks OK`);