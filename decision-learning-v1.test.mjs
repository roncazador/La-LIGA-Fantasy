import fs from 'node:fs';import assert from 'node:assert/strict';
const ui=fs.readFileSync('decision-learning-v1.js','utf8');let n=0;const c=(ok,msg)=>{n++;assert.ok(ok,`DL-${String(n).padStart(2,'0')}: ${msg}`)};
c(ui.includes('laliga_decision_learning_v2'),'memoria local versionada v2');c(ui.includes('OLD_KEY')&&ui.includes('laliga_decision_learning_v1'),'migración compatible de memoria v1');c(ui.includes('La seguí')&&ui.includes('No la seguí'),'seguimiento de recomendación');c(ui.includes('Decisión tomada'),'decisión estructurada');c(ui.includes('Motivo'),'motivo');c(ui.includes('Puntos finales'),'resultado final');c(ui.includes('Cerrar jornada y aprender'),'cierre de jornada');c(ui.includes("status:'pending-final'"),'estado pendiente');c(ui.includes("status:'final'")||ui.includes("e.status='final'"),'estado final');c(ui.includes('dlpending'),'selección explícita de pendiente');c(ui.includes('e.id===id&&e.status===\'pending-final\''),'cierre atado al evento correcto');c(ui.includes('outcomeSource=\'user-entered-final\''),'origen del resultado identificado');c(ui.includes('function learn('),'aprendizaje aislado');c(ui.includes('followRate')&&ui.includes('goodRate'),'métricas de decisión');c(ui.includes('trust')&&ui.includes('confidence'),'confianza y tamaño de muestra');c(ui.includes('decisionLearningToggle'),'rama accesible desde pestañas');c(ui.includes("r.style.display=show?'block':'none'"),'rama oculta por defecto');c(ui.includes('no escriben directamente en los pesos'),'protección de pesos del Cerebro');c(ui.includes("pts<0||pts>1000"),'validación de puntos');c(ui.includes('Selecciona primero si seguiste'),'no guarda decisión ambigua');c(ui.includes('week:ctx.week'),'contexto de jornada');console.log(`decision-learning-v2: ${n}/${n} checks OK`);