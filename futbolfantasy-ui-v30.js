(()=>{'use strict';const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const r=await fetch('/api/futbolfantasy/data',{credentials:'include',cache:'no-store'});if(!r.ok)throw Error(`HTTP_${r.status}`);return r.json()}function draw(data){const host=document.getElementById('datos');if(!host)return;let box=document.getElementById('ffData30');if(!box){box=document.createElement('div');box.id='ffData30';host.prepend(box)}box.className='card';box.innerHTML=`<div class="flex" style="justify-content:space-between"><div><h3 style="margin:0">🌐 FutbolFantasy</h3><div class="tiny">Datos públicos de contraste · solo lectura</div></div><span class="pill">${data.availablePages?.length||0} páginas</span></div><div class="three" style="margin-top:9px"><div class="kpi"><div class="label">Calendario</div><div class="value">${esc(data.counts?.calendar??0)}</div></div><div class="kpi"><div class="label">Enlaces</div><div class="value">${esc(data.counts?.links??0)}</div></div><div class="kpi"><div class="label">Títulos</div><div class="value">${esc(data.counts?.headings??0)}</div></div></div><div class="note" style="margin-top:9px">Se incorporan automáticamente las páginas públicas disponibles. La aplicación no convierte este contenido en datos oficiales de LALIGA.</div>${Array.isArray(data.pages)&&data.pages.length?`<div style="margin-top:9px">${data.pages.map(p=>`<div class="provider"><span>${esc(p.path)}</span><b>${esc(p.status)}</b></div>`).join('')}</div>`:''}`}
async function boot(){let tries=0;const run=async()=>{if(!document.getElementById('datos')&&tries++<40)return setTimeout(run,250);try{draw(await load())}catch{const host=document.getElementById('datos');if(host){let box=document.getElementById('ffData30');if(!box){box=document.createElement('div');box.id='ffData30';box.className='card';host.prepend(box)}box.innerHTML='<h3>🌐 FutbolFantasy</h3><div class="note">Fuente pública temporalmente no disponible. Se reintentará automáticamente.</div>'}}};run();setInterval(()=>load().then(draw).catch(()=>{}),10*60*1000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();
