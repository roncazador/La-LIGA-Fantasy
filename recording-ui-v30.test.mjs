import assert from 'node:assert/strict';
import fs from 'node:fs';
const files=p=>fs.readFileSync(p,'utf8');const recording=files('recording-client.js'),dataClient=files('data-client.js'),dashboard=files('dashboard-client.js'),connection=files('connection-client.js'),calendar=files('calendar-client.js'),autonomous=files('calendar-autonomous-v30.js'),calendar35=files('calendar-autonomous-v35.js'),dynamics=files('app-dynamics-v37.js'),focus=files('focus-ui-v30.js'),ffui=files('futbolfantasy-ui-v30.js'),config=files('config.mjs'),index=files('index.html'),sw=files('sw.js'),pkg=JSON.parse(files('package.json')),brainCore=files('brain-core-v27.mjs'),brainHost=files('brain-host-v27.mjs'),brainClient=files('brain-client-v27.js'),reliability=files('brain-reliability-v29.mjs'),reliabilityHook=files('brain-reliability-hook-v29.mjs'),render=files('render.yaml');
const checks=[];const check=(name,ok)=>checks.push([name,Boolean(ok)]);
check('package v2.15',pkg.version==='2.15.0');check('recording client existe',recording.length>0);check('data client existe',dataClient.length>0);check('legacy calendar disabled',calendar.includes('LALIGA_CALENDAR_LEGACY_DISABLED'));check('autonomous calendar active',autonomous.includes('/api/calendar/autonomous'));check('current calendar active',calendar35.includes('/api/calendar/autonomous'));check('automatic loading',calendar35.includes('Calendario LaLiga')&&calendar35.includes('MutationObserver')&&calendar35.includes('void refresh()'));check('live state',calendar35.includes('EN DIRECTO'));check('score rendering',calendar35.includes('homeScore')&&calendar35.includes('awayScore'));check('autonomous refresh',calendar35.includes('REFRESH_MS=15000'));check('current jornada inference',calendar35.includes('inferCurrentMatchday')&&calendar35.includes('localDay(now)'));check('persistent DOM mount',calendar35.includes('new MutationObserver')&&!calendar35.includes("host.innerHTML=''"));check('no manual calendar controls',!calendar35.includes('Cargar desde proveedor')&&!calendar35.includes('Usar calendario oficial'));check('dynamic shell v37',dynamics.includes("VERSION='3.7.0'")&&dynamics.includes('#dyn37ops'));check('dynamic automation recovery',dynamics.includes('RETRY_DELAYS=[5000,15000,30000]')&&dynamics.includes('clearRetry()'));check('shared calendar event',calendar35.includes("laliga:calendar-updated")&&dynamics.includes("laliga:calendar-updated"));check('shared refresh interval',dynamics.includes('REFRESH_MS=15000')&&calendar35.includes('REFRESH_MS=15000'));check('large navigation',focus.includes('min-height:62px'));check('dashboard legacy disabled',dashboard.includes('FANTASY_LEGACY_DASHBOARD_DISABLED'));check('connection legacy disabled',connection.includes('LALIGA_LEGACY_CONNECTION_DISABLED'));check('connection boots dynamics v37 before calendar',connection.indexOf('app-dynamics-v37.js')<connection.indexOf('calendar-autonomous-v35.js'));check('connection boots FF UI',connection.includes('futbolfantasy-ui-v30.js'));check('FF UI exists',ffui.includes('/api/futbolfantasy/data'));check('index responsive',index.includes('@media(max-width:560px)'));check('index tactile',index.includes('overflow:auto'));check('config public dynamics v37',config.includes("'/app-dynamics-v37.js'"));check('config public calendar seed',config.includes("'/official-fixtures-seed-2026-27.json'"));check('service worker cache v308',sw.includes("fm-v308"));check('service worker caches dynamics v37',sw.includes("./app-dynamics-v37.js"));check('service worker caches current calendar v35',sw.includes("./calendar-autonomous-v35.js"));check('service worker caches FF UI',sw.includes("./futbolfantasy-ui-v30.js"));check('service worker caches match detail',sw.includes("./match-detail-ui-v31.js"));check('brain core exists',brainCore.includes('class BrainV27'));check('brain persistent model',brainCore.includes('model-v27.json'));check('brain host status',brainHost.includes('/api/brain/status'));check('autonomous calendar endpoint',brainHost.includes('/api/calendar/autonomous'));check('futbolfantasy adapter',brainHost.includes('fetchFutbolFantasy'));check('futbolfantasy data route',brainHost.includes('/api/futbolfantasy/data'));check('brain client visible',brainClient.includes('brain27Panel'));check('reliability core visible',reliability.includes('assessPredictionReliability'));check('reliability hook loaded',reliabilityHook.includes('BrainV27.prototype.predict'));check('Render storage contract',render.includes('BRAIN_STATE_DIR')&&render.includes('/var/data/brain'));const secret=/(?:x-apisports-key|YOUR_API_KEY|Bearer\s+[A-Za-z0-9._-]{20,}|apiFootballKey\s*[:=]\s*['"][A-Za-z0-9._-]{20,}['"])/i;check('calendar no secrets',!secret.test(calendar35));check('dynamics no secrets',!secret.test(dynamics));check('data no secrets',!secret.test(dataClient));check('recording no secrets',!secret.test(recording));check('FutbolFantasy UI no secrets',!secret.test(ffui));check('SW includes no API credentials',!secret.test(sw));
assert.equal(checks.length,48,`Se esperaban 48 comprobaciones y hay ${checks.length}`);for(const[i,[name,ok]]of checks.entries())assert.ok(ok,`V37-${String(i+1).padStart(3,'0')}: ${name}`);console.log('✅ V37: 48/48 comprobaciones superadas');
